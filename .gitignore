# Compiled source #
###################
*.com
*.class
*.o
*.so
__pycache__/

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.rar
*.tar
*.nupkg

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Pycharm artifacts
###################
.idea

# vscode
# #################
.vscode/

# local upstream PR review log (fork-only, not published)
UPSTREAM_PR_REVIEW.md

# local Claude Code agent roles (fork-only, not published)
.claude/

# local Claude project instructions (fork-only, not published)
CLAUDE.md

# local upstream issue triage log (fork-only, not published)
ISSUE_REVIEW.md
